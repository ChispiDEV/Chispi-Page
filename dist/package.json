{
  "name": "chispi-page",
  "version": "1.0.0",
  "type": "module",
  "description": "Portfolio personal de Chispi",
  "scripts": {
    "dev": "node build.js --watch",
    "build": "set NODE_ENV=production&& node build.js",
    "build:dev": "set NODE_ENV=development&& node build.js",
    "serve": "npx serve dist -p 3000",
    "clean": "if exist dist rmdir /s dist && mkdir dist",
    "deploy": "npm run build && echo 🚀 Listo para deploy"
  },
  "devDependencies": {
    "sass": "^1.66.1",
    "chokidar": "^3.5.3",
    "fs-extra": "^11.1.1",
    "html-minifier-terser": "^7.2.0",
    "cssnano": "^6.0.1",
    "terser": "^5.24.0",
    "serve": "^14.2.1",
    "glob": "^10.3.10"
  }
}